[build]
buildCommand = "python -m pip install --upgrade pip && python -m pip install -r webapp/backend/requirements.txt"
watchPatterns = ["/webapp/backend/**", "/webapp/backend/railway.toml"]

[deploy]
startCommand = "python -m uvicorn webapp.backend.main:app --host 0.0.0.0 --port $PORT"
